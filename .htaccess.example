# Example .htaccess with security headers (Apache)
# Place in the site's root. These headers are suggestions — configure according to your environment.

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Referrer-Policy "no-referrer-when-downgrade"
  Header set Permissions-Policy "geolocation=()"
  # Content-Security-Policy example: allow same-origin for scripts/styles, and specific image sources
  Header always set Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'self';"
</IfModule>

# Note: CSP header should be tailored to your analytics, CDN, or third-party services.
