## .htaccess.example — Recommended security headers for Apache
# Place these in your server config or .htaccess (prefer server config) — adapt before use.

# Prevent MIME sniffing
Header set X-Content-Type-Options "nosniff"

# Clickjacking protection
Header set X-Frame-Options "SAMEORIGIN"

# Referrer policy
Header set Referrer-Policy "strict-origin-when-cross-origin"

# Reduce information in Server header (adjust per host)
ServerSignature Off

# Permissions-Policy (replace features as needed)
Header set Permissions-Policy "geolocation=(), microphone=()"

# Example Content-Security-Policy: adjust according to your asset hosts and analytics
# For strict CSP, set this header from the server and avoid inline scripts/styles.
# This is a conservative example and may need changes for external services.
Header set Content-Security-Policy "default-src 'self'; img-src 'self' data: https:; script-src 'self' https:; style-src 'self' https: 'unsafe-inline'; connect-src 'self' https:; frame-ancestors 'self';"
