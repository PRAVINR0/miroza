name: generate-site (moved-to-backup)

# This workflow was intentionally disabled and moved to `/backup/old_unused/generate-site.yml`
# on 2025-11-23 as part of a conservative cleanup. The original full workflow is preserved
# in the backup folder. To restore, move the file back and review carefully before re-enabling.
