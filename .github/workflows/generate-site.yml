name: Generate site metadata (disabled)

# This workflow has been disabled by repository cleanup performed on 2025-11-23.
# The original workflow was copied to `/backup/old_unused/generate-site.yml`.
# To restore automation, move the backup file back to `.github/workflows/` and review.
