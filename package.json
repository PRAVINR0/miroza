{
  "name": "miroza",
  "version": "1.0.0",
  "description": "MIROZA is a modern, performant, accessible news / articles / blog platform scaffold.",
  "main": "sw.js",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node ./tools/static-server.js",
    "check-links": "node ./tools/check-links.js",
    "smoke-test": "node ./tools/smoke-test.js",
    "add-defer": "node ./tools/add-defer-to-scripts.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:data && npm run build:css && npm run build:js",
    "build:data": "node tools/refresh-posts.js",
    "build:css": "esbuild styles/styles.css --minify --outfile=styles/styles.min.css",
    "build:js": "esbuild scripts/app.js --minify --target=es2018 --legal-comments=none --outfile=scripts/app.min.js",
    "build:js-wasm": "node tools/build-wasm.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PRAVINR0/miroza.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PRAVINR0/miroza/issues"
  },
  "homepage": "https://github.com/PRAVINR0/miroza#readme",
  "dependencies": {
    "puppeteer": "^22.11.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "esbuild-wasm": "^0.18.10",
    "puppeteer": "^22.11.1",
    "lighthouse": "^10.0.0"
  }
}
