{
  "name": "miroza-site-tools",
  "version": "0.0.0",
  "description": "Site generation and helper scripts for the Miroza static site",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "generate": "node scripts/generate_site.js",
    "build": "node scripts/build.js",
    "optimize-images": "node scripts/optimize-images.js",
    "start": "node server.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "sharp": "^0.32.0"
  },
  "devDependencies": {
    "terser": "^5.20.0",
    "clean-css": "^5.3.0"
  }
}
