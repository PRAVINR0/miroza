<# normalize_data.ps1 (disabled)
   Original moved to /backup/old_unused/scripts/normalize_data.ps1
   This helper is disabled in the static-only conversion. Review the backup before re-enabling.
#>
Write-Output 'normalize_data.ps1 disabled — original moved to /backup/old_unused/scripts/normalize_data.ps1'
