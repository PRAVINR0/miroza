/* vendor.js
   Placeholder for third-party libs. For production include libs with SRI and pin versions.
   Example: DOMPurify for sanitizing rich HTML (if needed).

   <script src="https://cdn.jsdelivr.net/npm/dompurify@2.4.0/dist/purify.min.js"
     integrity="sha384-..." crossorigin="anonymous"></script>
*/
// Vendor stubs (no dependencies required for the base template)
window.MIROZA_VENDORS = {
  // attach vendor helpers here when needed
};
